#include <stdio.h>

int main() {
    int a = 10;
    float b = 3.14;
    char c = 'A';
    
    if (a > 5) {
        printf("a is greater than 5\n");
        for (int i = 0; i < 3; i++) {
            printf("Loop iteration: %d\n", i);
        }
    } else {
        printf("a is less than or equal to 5\n");
    }
    
    while (a > 0) {
        a--;
        printf("a = %d\n", a);
    }
    
    return 0;
}